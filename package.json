{
  "name": "lounge-launcher",
  "version": "0.0.1",
  "description": "Personal fullscreen launcher for LG webOS TVs",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "pack": "node scripts/build.js --pack",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@procot/webostv": "^1.2.6",
    "esbuild": "^0.25.0"
  }
}
